<script setup lang="ts">
import type { RouterLink } from '@/router/list-routes';

interface Props {
        title?: string;
        links: RouterLink[];
        isSecondary?: boolean
    }
const props = withDefaults(defineProps<Props>(), {
    title:'CampoApp',
    isSecondary: false
})
</script>

<template>
    <nav>
      <template v-if="!props.isSecondary">
        <img alt="Vue logo" class="logo" src="@/assets/logo.svg" width="25" height="25" />
        <span v-if="props">{{props.title}}</span>
          <RouterLink 
            v-for="link in props.links"
            :key="link.path"
            :to="link.path"
          >
              {{ link.title }}
        </RouterLink>>
      </template>

        <template v-else>
          <RouterLink v-for="link in props.links"
          :key="link.path"
          :to="link.path">
            {{  link.title }}
        </RouterLink>
        </template>
        
                <!--<RouterLink to="/">Home</RouterLink>-->

    </nav>



       

</template>



<style scoped>
header {
  line-height: 1.5;
  max-height: 100vh;
}

.logo {
  display: block;
  margin: 0 auto 2rem;
}

nav {
  width: 100%;
  font-size: 12px;
  text-align: center;
  margin-top: 2rem;
}

nav a.router-link-active {
  color: var(--color-text);
}

nav a.router-link-exact-active:hover {
  background-color: transparent;
}

nav a {
  display: inline-block;
  padding: 0 1rem;
  border-left: 1px solid var(--color-border);
}

nav a:first-of-type {
  border: 0;
}

@media (min-width: 1024px) {
  header {
    display: flex;
    place-items: center;
    padding-right: calc(var(--section-gap) / 2);
  }

  .logo {
    margin: 0 2rem 0 0;
  }

  header .wrapper {
    display: flex;
    place-items: flex-start;
    flex-wrap: wrap;
  }

  nav {
    text-align: left;
    margin-left: -1rem;
    font-size: 1rem;

    padding: 1rem 0;
    margin-top: 1rem;
  }
}
</style>
